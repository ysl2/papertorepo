# papertorepo frontend

This directory contains the Vite/React WebUI for `papertorepo`.

Useful commands:

```bash
npm ci
npm run build
npm run dev
```

During local development, the Vite dev server proxies `/api` to `http://127.0.0.1:8000`.

# Papers table Published header width plan

## Summary

Widen the frontend Papers table `Published` column so the custom header can show the full label with sort and filter buttons.

## Implementation

- Increase the `Published` column preferred width.
- Bump the Papers grid persistence id to avoid stale saved column widths.

## Tests

- Run frontend build.
- Run `git diff --check`.

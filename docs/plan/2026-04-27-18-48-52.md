# Papers table updated column width and Journal Ref order plan

## Summary

Adjust the frontend Papers table column configuration so the GitHub `Updated` header has enough width and `Journal Ref` appears to the right of `Description`.

## Implementation

- Increase the Papers table `Updated` column preferred width.
- Move `Journal Ref` after `Description`.
- Bump the Papers grid persistence id to avoid stale saved column order.

## Tests

- Run frontend build.
- Run `git diff --check`.

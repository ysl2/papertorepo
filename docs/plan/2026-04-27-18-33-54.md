# Papers table empty metadata display plan

## Summary

Reduce visual noise in the Papers table by rendering empty repository metadata cells as blank instead of an em dash placeholder.

## Implementation

- Keep global date formatting unchanged for other UI surfaces.
- Add table-local date formatting for Papers table date columns that returns an empty string for empty values.
- Update repository size formatting to return an empty string when size metadata is missing.
- Verify with frontend build.

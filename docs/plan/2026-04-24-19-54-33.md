# repair resume stats copy cleanup

## Summary

Clean up `sync-papers` repair resume UI copy so job stats do not repeat the same checkpoint information twice. The backend resume payload and execution behavior remain unchanged.

## Key Changes

- Fix irregular plural copy for `metadata batch`.
- Prefer current execution checkpoint stats once a repair job has started.
- Show `Repair resume` only when current job stats are still empty or do not already contain checkpoint reuse information.
- Keep pending repair children informative by still showing their resume summary before execution starts.

## Test Plan

- Run frontend production build.
- Run backend tests to ensure the API payload change remains valid.
